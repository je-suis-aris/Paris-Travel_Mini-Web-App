# Paris-Travel_Mini-Web-App
Lightweight travel recommendation web application deployed on Linux using CGI shell scripts and a MariaDB backend. The site provides navigation pages for seasonal tips, an account creation flow that persists user data in MariaDB, a cost estimator that calculates an approximate budget based on user inputs and renders the result dynamically as HTML.
